import { createMuiTheme } from "@material-ui/core/styles";
import { red, teal, green, cyan, blue, purple } from "@material-ui/core/colors";

const theme = createMuiTheme({
  palette: {
    primary: teal,
    secondary: red
  }
});

export default theme;
